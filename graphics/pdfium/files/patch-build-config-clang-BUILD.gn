--- build/config/clang/BUILD.gn.orig	2025-01-01 00:00:00 UTC
+++ build/config/clang/BUILD.gn
@@ -184,6 +184,12 @@
         }
       } else if (is_apple) {
         _dir = "darwin"
+      } else if (current_os == "freebsd") {
+        if (current_cpu == "x64") {
+          _dir = "x86_64-portbld-freebsd15.0"
+        } else {
+          assert(false)  # Unhandled FreeBSD cpu type
+        }
       } else if (is_linux || is_chromeos) {
         if (current_cpu == "x64") {
           _dir = "x86_64-unknown-linux-gnu"
